---
phase: 01-correctness-build
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
autonomous: false
requirements:
  - DEPS-02
  - DEPS-03

must_haves:
  truths:
    - "vite-plugin-singlefile 2.3.2 (exact) is installed — not 2.3.3 or higher"
    - "Vite 7.x is installed — package.json shows ^7.0.0"
    - "@vitejs/plugin-react ^5.0.0 is installed — not ^4.x or ^6.x"
    - "npm run build:local completes without errors"
    - "The resulting dist-local/index.html opens in Chrome via file:// and the app renders"
  artifacts:
    - path: "frontend/package.json"
      provides: "Updated version pins for vite, @vitejs/plugin-react, vite-plugin-singlefile"
      contains: "\"vite\": \"^7.0.0\""
    - path: "frontend/vite.config.ts"
      provides: "Verified-compatible Vite config (likely no changes needed)"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/vite.config.ts"
      via: "vite defineConfig import"
      pattern: "defineConfig"

user_setup:
  - service: node
    why: "Vite 7 requires Node 20.19+. The host machine has Node 18 — npm run dev and npm run build will fail natively."
    env_vars: []
    dashboard_config:
      - task: "Use Docker for the build step"
        location: "Run: docker compose run --rm frontend npm run build:local"
---

<objective>
Pin vite-plugin-singlefile to exact 2.3.2, upgrade Vite from 5 to 7 and @vitejs/plugin-react from 4 to 5, then verify the offline single-file HTML build completes and opens in Chrome.

Purpose: DEPS-02 and DEPS-03 prevent future breakage from incompatible dependency drift and ensure the offline HTML distribution path works under Vite 7.

Output: Updated package.json, verified vite.config.ts (likely no changes), and a human checkpoint confirming the offline build loads via file:// in Chrome.

Node constraint: Vite 7 requires Node >=20.19. The project's Docker frontend service uses node:20-alpine (satisfies requirement). Native npm commands on a host with Node 18 will fail. Use Docker or upgrade host Node to 20.19+.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-correctness-build/01-CONTEXT.md
@.planning/phases/01-correctness-build/01-RESEARCH.md

<interfaces>
<!-- Current package.json devDependencies (lines 20-28): -->
```json
"@vitejs/plugin-react": "^4.3.1",
"autoprefixer": "^10.4.19",
"postcss": "^8.4.40",
"tailwindcss": "^3.4.7",
"typescript": "^5.5.4",
"vite": "^5.3.5",
"vite-plugin-singlefile": "^2.0.2"
```

<!-- Current vite.config.ts (full file — no Vite 5-specific APIs): -->
```typescript
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";
import { viteSingleFile } from "vite-plugin-singlefile";

const isSingleFile = process.env.VITE_STORAGE === "local";

export default defineConfig({
  plugins: [react(), ...(isSingleFile ? [viteSingleFile()] : [])],
  build: isSingleFile
    ? { assetsInlineLimit: Infinity, cssCodeSplit: false }
    : {},
  server: { port: 5173, strictPort: true },
});
```

<!-- This config uses no deprecated Vite 5-only APIs. build.rollupOptions is absent
     (Vite 7 still uses rollupOptions — build.rolldownOptions is Vite 8+ only).
     vite.config.ts should work unchanged after the package.json update. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json version pins and run npm install</name>
  <files>frontend/package.json, frontend/vite.config.ts</files>
  <read_first>
    - frontend/package.json (current full content — to confirm exact current pins before editing)
    - frontend/vite.config.ts (current full content — to verify no Vite 5-specific APIs need removal)
    - .planning/phases/01-correctness-build/01-RESEARCH.md (Pitfall 2 re Node 20.19+, Pitfall 3 re exact vite-plugin-singlefile pin, Standard Stack table)
  </read_first>
  <action>
Modify `frontend/package.json` — update three devDependency entries:
- `"@vitejs/plugin-react"`: change from `"^4.3.1"` to `"^5.0.0"`
- `"vite"`: change from `"^5.3.5"` to `"^7.0.0"`
- `"vite-plugin-singlefile"`: change from `"^2.0.2"` to `"2.3.2"` (exact — no caret, no tilde)

Do not touch any other fields in package.json.

Read `frontend/vite.config.ts` and verify it contains no Vite 5-specific deprecated APIs:
- Confirm `build.rollupOptions` is absent (it is — confirmed in patterns file)
- Confirm `splitVendorChunkPlugin` is not imported (it is not)
- Confirm `assetsInlineLimit`, `cssCodeSplit`, `server.port`, `server.strictPort` are all valid Vite 7 config keys (they are)

If vite.config.ts requires no changes, do not modify it — just read it and confirm.

After editing package.json, run npm install inside the Docker frontend service OR note that the executor should run:
```
docker compose run --rm frontend npm install
```
or, if host Node >=20.19 is available:
```
cd /home/spro/development/MentalHealth/frontend && npm install
```

The npm install step resolves Vite 7.x and vite-plugin-singlefile 2.3.2 exactly.
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/frontend && npm list vite vite-plugin-singlefile @vitejs/plugin-react 2>/dev/null | grep -E "vite@|vite-plugin-singlefile@|plugin-react@" || echo "run: docker compose run --rm frontend npm list vite vite-plugin-singlefile @vitejs/plugin-react"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"vite": "^7.0.0"`
    - `frontend/package.json` contains `"vite-plugin-singlefile": "2.3.2"` (no caret)
    - `frontend/package.json` contains `"@vitejs/plugin-react": "^5.0.0"`
    - After npm install: `npm list vite-plugin-singlefile` shows `2.3.2` (not `2.3.3`)
    - After npm install: `npm list vite` shows a `7.x.x` version
    - `vite.config.ts` is unchanged (no modifications needed)
  </acceptance_criteria>
  <done>package.json updated; npm install resolves Vite 7 and singlefile 2.3.2</done>
</task>

<task type="auto">
  <name>Task 2: Run offline build and typecheck</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json (verify build:local script is `tsc --noEmit && VITE_STORAGE=local vite build --outDir dist-local`)
    - frontend/vite.config.ts (confirm viteSingleFile plugin is active when VITE_STORAGE=local)
  </read_first>
  <action>
Run the offline build to confirm Vite 7 + vite-plugin-singlefile 2.3.2 work together:

```bash
docker compose run --rm frontend npm run build:local
```

Or if host Node >=20.19 is available:
```bash
cd /home/spro/development/MentalHealth/frontend && npm run build:local
```

This runs `tsc --noEmit && VITE_STORAGE=local vite build --outDir dist-local`.

If the build succeeds, `frontend/dist-local/index.html` will exist as a single self-contained HTML file.

If the build fails with a Vite config error about deprecated APIs (unlikely given the current vite.config.ts), update vite.config.ts to remove the deprecated usage — document what was changed.

After the build succeeds, also run the standard typecheck:
```bash
cd /home/spro/development/MentalHealth/frontend && npm run typecheck
```
(or via Docker if host Node is incompatible)
  </action>
  <verify>
    <automated>ls /home/spro/development/MentalHealth/frontend/dist-local/index.html 2>/dev/null && echo "build output exists" || echo "build not yet run — run: docker compose run --rm frontend npm run build:local"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/dist-local/index.html` exists after build
    - `npm run build:local` exits 0 (no TypeScript errors, no Vite errors)
    - `npm run typecheck` exits 0
    - The build output is a single HTML file (not a directory with many chunks) — `ls -lh frontend/dist-local/` should show only `index.html` or very few files
  </acceptance_criteria>
  <done>Offline build completes; dist-local/index.html exists; typecheck clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package-lock.json | Version pins prevent auto-upgrade to incompatible vite-plugin-singlefile 2.3.3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | vite-plugin-singlefile exact pin | mitigate | Exact pin `2.3.2` (no caret) prevents automatic upgrade to 2.3.3 which is untested against Vite 7 in this project |
</threat_model>

<verification>
1. `grep '"vite": "\^7' frontend/package.json` — matches
2. `grep '"vite-plugin-singlefile": "2.3.2"' frontend/package.json` — matches (no caret)
3. `npm run build:local` (via Docker or Node 20+) — exits 0
4. `ls frontend/dist-local/index.html` — file exists

Manual checkpoint below required before plan is considered complete.
</verification>

<success_criteria>
- `frontend/package.json` has all three version pins updated
- Offline build completes without errors (verified by human in checkpoint below)
- `dist-local/index.html` loads in Chrome via `file://` (verified by human in checkpoint below)
</success_criteria>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Updated package.json pins (Vite 7, @vitejs/plugin-react 5, vite-plugin-singlefile 2.3.2 exact). Offline HTML build run via `npm run build:local`. Output at `frontend/dist-local/index.html`.
  </what-built>
  <how-to-verify>
    1. Open `frontend/dist-local/index.html` in Chrome using File > Open File (or drag into Chrome)
    2. Confirm the app loads — you should see the Kompass navigation sidebar
    3. Click at least one module (e.g., "Werte") — confirm it renders without a blank screen or console errors
    4. Open DevTools Console — confirm no red errors related to Vite, module loading, or CORS
    5. (Nice-to-have, not blocking per D-10): Repeat in Safari
  </how-to-verify>
  <resume-signal>Type "approved" if the app loads and renders correctly via file://, or describe any errors you see</resume-signal>
</task>

<output>
After completion, create `.planning/phases/01-correctness-build/01-04-SUMMARY.md`
</output>
