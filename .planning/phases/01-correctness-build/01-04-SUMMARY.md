---
phase: 01-correctness-build
plan: "04"
subsystem: infra
tags: [vite, vite-plugin-singlefile, offline-build, dependencies, node]

# Dependency graph
requires:
  - phase: 01-01
    provides: backend test infrastructure (conftest, pytest config)
  - phase: 01-02
    provides: backend correctness fixes applied
  - phase: 01-03
    provides: frontend correctness fixes applied
provides:
  - Vite 7.x installed and verified working in the project
  - vite-plugin-singlefile pinned to exact 2.3.2 (no-caret)
  - Offline single-file HTML build confirmed via file:// in Firefox
affects: [02-content-gaps, 03-data-portability, 04-snapshot-system]

# Tech tracking
tech-stack:
  added:
    - "vite ^7.0.0 (upgraded from ^5.3.5)"
    - "@vitejs/plugin-react ^5.0.0 (upgraded from ^4.3.1)"
    - "vite-plugin-singlefile 2.3.2 (exact pin, upgraded from ^2.0.2)"
  patterns:
    - "Exact pin (no caret) for vite-plugin-singlefile to prevent auto-upgrade to incompatible 2.3.3+"
    - "Offline build uses VITE_STORAGE=local env var to activate viteSingleFile() plugin"

key-files:
  created: []
  modified:
    - frontend/package.json
    - frontend/package-lock.json

key-decisions:
  - "vite-plugin-singlefile pinned without caret (2.3.2, not ^2.3.2) — 2.3.3 is untested against Vite 7 in this project (T-01-08)"
  - "vite.config.ts required no changes — existing config is Vite 7 compatible (no deprecated APIs used)"
  - "Build was run via Docker (node:20-alpine) because host Node was 18; Vite 7 requires Node >=20.19"

patterns-established:
  - "Offline build: docker compose run --rm frontend npm run build:local produces dist-local/index.html"
  - "Exact pin strategy: dependencies with known breaking point get exact version (no range specifier)"

requirements-completed:
  - DEPS-02
  - DEPS-03

# Metrics
duration: ~20min
completed: 2026-04-21
---

# Phase 01 Plan 04: Vite 7 + Singlefile Pin Summary

**Vite upgraded from 5 to 7, @vitejs/plugin-react from 4 to 5, vite-plugin-singlefile exact-pinned at 2.3.2; offline HTML build verified loading via file:// in Firefox**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-04-21T09:32Z
- **Completed:** 2026-04-21 (after human checkpoint approval)
- **Tasks:** 2 (+ 1 human-verify checkpoint)
- **Files modified:** 2 (package.json, package-lock.json)

## Accomplishments
- Updated three devDependency pins in `frontend/package.json`: Vite 5→7, plugin-react 4→5, singlefile ^2.0.2→2.3.2 (exact)
- Ran `npm install` via Docker to resolve new versions
- Ran `npm run build:local` — `dist-local/index.html` produced as a single 648 KB self-contained HTML file
- Human checkpoint: user opened `dist-local/index.html` in Firefox via `file://` and confirmed the app renders correctly (sidebar navigation visible, modules load, no console errors)

## Task Commits

1. **Task 1+2: Update package.json pins and run offline build** - `28274a5` (chore)
2. **State update** - `3342ea7` (docs)

## Files Created/Modified
- `frontend/package.json` - Updated: `vite ^7.0.0`, `@vitejs/plugin-react ^5.0.0`, `vite-plugin-singlefile 2.3.2`
- `frontend/package-lock.json` - Regenerated by npm install with new version tree

## Decisions Made
- `vite-plugin-singlefile` pinned without caret (`"2.3.2"` not `"^2.3.2"`) — the 2.3.3 release is untested and the plugin has a history of breaking changes against Vite minor versions
- `vite.config.ts` was verified as Vite 7 compatible without modification: uses `defineConfig`, `react()`, `viteSingleFile()`, `assetsInlineLimit: Infinity`, `cssCodeSplit: false`, `server.port`, `server.strictPort` — all remain valid Vite 7 config keys
- Docker build path (`docker compose run --rm frontend`) required because the host machine runs Node 18, below Vite 7's minimum of Node 20.19

## Deviations from Plan

None — plan executed exactly as written. `vite.config.ts` required no modifications as predicted by the plan's interface documentation.

## Issues Encountered
None. The build path via Docker worked cleanly on first attempt. The human checkpoint was approved without issue.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Phase 1 is fully complete (all 4 plans executed and verified)
- DEPS-02 and DEPS-03 requirements are satisfied
- The offline distribution path is functional — `npm run build:local` produces a working standalone HTML app
- Phase 2 (Content Gaps) can proceed: the build toolchain is stable at Vite 7

---
*Phase: 01-correctness-build*
*Completed: 2026-04-21*
