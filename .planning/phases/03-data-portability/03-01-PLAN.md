---
phase: 03-data-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/portability.py
  - backend/tests/test_portability.py
  - backend/app/main.py
autonomous: true
requirements:
  - PORT-01
  - PORT-02

must_haves:
  truths:
    - "GET /api/export gibt ein JSON-Objekt mit _version=1, _exported-Timestamp und einem Schlüssel pro gespeichertem Modul zurück"
    - "GET /api/export liefert nur Daten des authentifizierten Users (user_id-Filter)"
    - "POST /api/import schreibt rohe Blobs in module_records — kein Pydantic-Validate, kein Registry-Check"
    - "POST /api/import akzeptiert Modul-IDs, die nicht im Registry registriert sind (z.B. 'orientation' aus HTML-v1)"
    - "Import→GET-Roundtrip: nach POST /api/import sind die Daten über GET /api/modules/{id} abrufbar"
  artifacts:
    - path: "backend/app/routers/portability.py"
      provides: "GET /api/export und POST /api/import Endpoints"
      exports: ["router"]
    - path: "backend/tests/test_portability.py"
      provides: "Integration-Tests für PORT-01 und PORT-02"
      contains: "test_export_format, test_export_all_modules, test_import_stores_blob, test_import_html_v1_compat, test_import_roundtrip"
    - path: "backend/app/main.py"
      provides: "portability.router registriert"
      contains: "portability"
  key_links:
    - from: "backend/app/routers/portability.py"
      to: "backend/app/models.ModuleRecord"
      via: "SQLModel select + session.add"
      pattern: "select(ModuleRecord).where(ModuleRecord.user_id == user_id)"
    - from: "backend/app/main.py"
      to: "backend/app/routers/portability.py"
      via: "app.include_router(portability.router)"
      pattern: "include_router(portability"
---

<objective>
Implementiere die zwei Backend-Portabilitäts-Endpoints und ihre Integration-Tests.

Purpose: Server-Modus-Nutzer können ihre Daten als HTML-v1-kompatibles JSON exportieren und bestehende HTML-v1-Exporte importieren.
Output: `backend/app/routers/portability.py` mit zwei Endpoints, `backend/tests/test_portability.py` mit fünf Tests, `backend/app/main.py` mit Router-Registrierung.
</objective>

<execution_context>
@/home/spro/.claude/get-shit-done/workflows/execute-plan.md
@/home/spro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-data-portability/03-RESEARCH.md
@.planning/phases/03-data-portability/03-PATTERNS.md

<interfaces>
<!-- Aus backend/app/routers/modules.py — direkt wiederverwendbare Patterns -->

Imports-Pattern (vollständig):
```python
from __future__ import annotations

import logging
from datetime import datetime, timezone
from uuid import UUID

from fastapi import APIRouter, Body, Depends
from sqlmodel import Session, select

from ..auth import current_user_id
from ..db import get_session
from ..models import ModuleRecord
```

Router-Deklaration:
```python
router = APIRouter(prefix="/api", tags=["portability"])
```

Auth + Session Dependency-Pattern (Signatur für beide Endpoints):
```python
def export_all(
    session: Session = Depends(get_session),
    user_id: UUID = Depends(current_user_id),
) -> dict:
```

UPSERT-Pattern aus modules.py (für Import anpassen):
```python
now = datetime.now(timezone.utc)
if record is None:
    record = ModuleRecord(
        user_id=user_id,
        module_id=module_id,
        schema_version=spec.schema_version,  # ← ersetzen durch: int(entry["schema_version"])
        data=normalized,                       # ← ersetzen durch: entry["data"]
        created_at=now,
        updated_at=now,
    )
else:
    record.schema_version = spec.schema_version  # ← ersetzen durch: int(entry["schema_version"])
    record.data = normalized                      # ← ersetzen durch: entry["data"]
    record.updated_at = now
session.add(record)
session.commit()
# KEIN session.refresh() beim Import — kein Response-Objekt nötig
```

Aus backend/app/main.py (aktuelle Router-Registrierung):
```python
from .routers import health, modules      # ← portability hinzufügen
app.include_router(modules.router)        # ← darunter: app.include_router(portability.router)
```

Aus backend/tests/conftest.py (Fixtures, kein eigener Aufwand):
```python
# Alle drei Fixtures sind nutzbar:
# @pytest.fixture def test_engine()     — in-memory SQLite
# @pytest.fixture async def client(test_engine)  — AsyncClient gegen FastAPI
# @pytest.fixture def auth_headers()    — {"Authorization": "Bearer <token>"}
```

Aus backend/tests/test_modules.py (Round-trip Testmuster):
```python
from __future__ import annotations
import pytest
from sqlmodel import Session, select
from app.models import ModuleRecord

@pytest.mark.asyncio
async def test_checkin_roundtrip(client, auth_headers):
    put_resp = await client.put("/api/modules/checkin", json={"entries": []}, headers=auth_headers)
    assert put_resp.status_code == 200
    get_resp = await client.get("/api/modules/checkin", headers=auth_headers)
    assert get_resp.status_code == 200
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-Stubs — test_portability.py erstellen (Wave-0-Gap)</name>
  <files>backend/tests/test_portability.py</files>
  <read_first>
    - backend/tests/conftest.py (Fixtures: client, auth_headers, test_engine)
    - backend/tests/test_modules.py (Testmuster: Round-trip, Direktzugriff auf DB via test_engine)
    - .planning/phases/03-data-portability/03-VALIDATION.md (Testrequirements je Req-ID)
  </read_first>
  <behavior>
    - test_export_format: GET /api/export → Response enthält Schlüssel "_version" == 1 und "_exported" (string)
    - test_export_all_modules: PUT /api/modules/values mit {"selected":[],"intentions":[],"reflection":""}, dann GET /api/export → Response enthält Schlüssel "values" mit "schema_version" und "data"
    - test_import_stores_blob: POST /api/import mit {"values": {"schema_version": 1, "data": {"selected":[],"intentions":[],"reflection":""}}} → status 200, Response enthält {"imported": ["values"], "skipped": []}; Direktabfrage via test_engine bestätigt ModuleRecord.module_id == "values" in DB
    - test_import_html_v1_compat: POST /api/import mit dem HTML-v1-Format inklusive unbekannter Modul-ID "orientation" ({"schema_version":1,"data":{"responses":[]},"updated_at":"2026-04-20T16:29:28.165Z"}) und bekannter "values" → beide in Response["imported"], kein Fehler; "orientation" landet trotz fehlendem Registry-Eintrag in der DB
    - test_import_roundtrip: POST /api/import mit values-Payload, dann GET /api/modules/values → Response["data"]["selected"] == [] (Daten sichtbar nach Reload)
  </behavior>
  <action>
    Erstelle `backend/tests/test_portability.py` mit den fünf Testfunktionen oben.

    Datei-Header (exakt übernehmen aus test_modules.py-Muster):
    ```python
    from __future__ import annotations

    import pytest
    from sqlmodel import Session, select

    from app.models import ModuleRecord
    ```

    Alle Tests sind `async def` mit `@pytest.mark.asyncio` und nutzen `client` + `auth_headers` Fixtures.
    `test_engine` nur in Tests hinzufügen, die direkten DB-Zugriff benötigen (`test_import_stores_blob`, `test_import_html_v1_compat`).

    Für `test_import_html_v1_compat`: Payload ist:
    ```python
    {
        "_version": 1,
        "_exported": "2026-04-20T16:34:01.372Z",
        "orientation": {"schema_version": 1, "data": {"responses": []}, "updated_at": "2026-04-20T16:29:28.165Z"},
        "values": {"schema_version": 1, "data": {"selected": [], "intentions": [], "reflection": ""}, "updated_at": "2026-04-20T19:43:25.869Z"},
    }
    ```
    Erwartung: `response.json()["imported"]` enthält "orientation" und "values" (Reihenfolge egal via `set()`-Vergleich); `response.json()["skipped"]` ist leer.

    Die Tests werden in Wave 0 als ROTE Tests geschrieben (kein portability.py existiert noch) — sie sollen beim ersten Run fehlschlagen. Das ist korrekt (TDD RED-Phase).
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/backend && uv run pytest tests/test_portability.py -q 2>&1 | tail -5</automated>
    Erwartetes Ergebnis nach Wave 0: ImportError oder 5 FAILED (kein portability-Modul existiert noch) — das ist korrekt.
  </verify>
  <done>
    `backend/tests/test_portability.py` existiert mit 5 Testfunktionen (test_export_format, test_export_all_modules, test_import_stores_blob, test_import_html_v1_compat, test_import_roundtrip). Tests fehlschlagen nur wegen fehlendem portability.py-Modul, nicht wegen Syntaxfehlern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend — portability.py implementieren + main.py registrieren</name>
  <files>backend/app/routers/portability.py, backend/app/main.py</files>
  <read_first>
    - backend/app/routers/modules.py (vollständiges Import-Pattern, Auth-Pattern, UPSERT-Pattern, Body-Parameter)
    - backend/app/main.py (aktuelle Router-Imports und include_router-Aufrufe)
    - backend/app/models.py (ModuleRecord-Felder: user_id, module_id, schema_version, data, created_at, updated_at)
    - backend/tests/test_portability.py (zum Verstehen, was die Tests erwarten)
  </read_first>
  <action>
    **portability.py erstellen** (`backend/app/routers/portability.py`):

    ```python
    """Export and import of all module data.

    GET  /api/export  — returns all module records in HTML-v1 flat format
    POST /api/import  — writes raw blobs without Pydantic validation; lazy migration on next GET
    """
    from __future__ import annotations

    import logging
    from datetime import datetime, timezone
    from uuid import UUID

    from fastapi import APIRouter, Body, Depends
    from sqlmodel import Session, select

    from ..auth import current_user_id
    from ..db import get_session
    from ..models import ModuleRecord

    logger = logging.getLogger(__name__)

    router = APIRouter(prefix="/api", tags=["portability"])


    @router.get("/export")
    def export_all(
        session: Session = Depends(get_session),
        user_id: UUID = Depends(current_user_id),
    ) -> dict:
        records = session.exec(
            select(ModuleRecord).where(ModuleRecord.user_id == user_id)
        ).all()
        out: dict = {
            "_version": 1,
            "_exported": datetime.now(timezone.utc).isoformat(),
        }
        for r in records:
            out[r.module_id] = {
                "schema_version": r.schema_version,
                "data": r.data,
                "updated_at": r.updated_at.isoformat() if r.updated_at else None,
            }
        return out


    @router.post("/import", status_code=200)
    def import_all(
        payload: dict = Body(...),
        session: Session = Depends(get_session),
        user_id: UUID = Depends(current_user_id),
    ) -> dict:
        imported: list[str] = []
        skipped: list[str] = []
        now = datetime.now(timezone.utc)

        for key, entry in payload.items():
            if key.startswith("_") or not isinstance(entry, dict):
                continue
            if "data" not in entry or "schema_version" not in entry:
                skipped.append(key)
                continue

            module_id = key
            try:
                schema_version = int(entry["schema_version"])
            except (TypeError, ValueError):
                skipped.append(key)
                continue
            data = entry["data"]

            record = session.exec(
                select(ModuleRecord).where(
                    ModuleRecord.user_id == user_id,
                    ModuleRecord.module_id == module_id,
                )
            ).first()

            if record is None:
                record = ModuleRecord(
                    user_id=user_id,
                    module_id=module_id,
                    schema_version=schema_version,
                    data=data,
                    created_at=now,
                    updated_at=now,
                )
            else:
                record.schema_version = schema_version
                record.data = data
                record.updated_at = now

            session.add(record)
            imported.append(module_id)

        session.commit()
        return {"imported": imported, "skipped": skipped}
    ```

    Wichtige Abweichungen von modules.py (absichtlich):
    - KEIN `get_module()`-Aufruf — unbekannte Modul-IDs werden akzeptiert (PORT-02)
    - KEINE Pydantic-Validierung — raw blob direkt in DB schreiben
    - KEIN `session.refresh()` — kein Response-Objekt nach Import nötig
    - `schema_version` als `int()` casten + try/except für korrupte Payloads

    **main.py anpassen** — zwei Zeilen ändern:
    1. Import-Zeile: `from .routers import health, modules` → `from .routers import health, modules, portability`
    2. Nach `app.include_router(modules.router)` einfügen: `app.include_router(portability.router)`
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/backend && uv run pytest tests/test_portability.py -q</automated>
    Alle 5 Tests grün. Danach: `uv run pytest tests/ -q` — volle Suite grün (keine Regressions).
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/portability.py` existiert und enthält `async def export_all(` ODER `def export_all(`
    - `grep -n "portability" backend/app/main.py` liefert mindestens 2 Treffer (Import-Zeile + include_router-Zeile)
    - `cd backend && uv run pytest tests/test_portability.py -q` exitiert mit Code 0 (alle 5 Tests grün)
    - `cd backend && uv run pytest tests/ -q` exitiert mit Code 0 (keine Regressions)
    - `grep -n "get_module" backend/app/routers/portability.py` liefert 0 Treffer (kein Registry-Check beim Import)
  </acceptance_criteria>
  <done>
    GET /api/export und POST /api/import existieren und sind im Router registriert. Alle 5 Portability-Tests grün. Volle Test-Suite grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Authentifizierter Client → /api/export | Bearer-Token-Check via `current_user_id`; ohne Token → 401 |
| Authentifizierter Client → /api/import | Beliebiges JSON aus Browser-Datei-Upload; Inhalt unvalidiert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | POST /api/import | mitigate | `user_id` ausschließlich aus `Depends(current_user_id)` — nie aus Payload lesen; alle geschriebenen Records gehören dem authentifizierten User |
| T-03-02 | Information Disclosure | GET /api/export | mitigate | `WHERE ModuleRecord.user_id == user_id` — SELECT filtert auf eigene Daten; single-user v1, aber Pattern ist korrekt für Multi-User |
| T-03-03 | Denial of Service | POST /api/import (großer Body) | accept | Starlette-Default-Limit ~1 MB; für Single-User-Reflexionstool ausreichend; kein explizites Limit nötig |
| T-03-04 | Tampering | POST /api/import (schema_version Manipulation) | accept | Lazy Migration auf next GET fängt korrupte schema_version-Werte ab (QUAL-04-Pattern bereits implementiert); `int()` Cast verhindert String-Injection |
</threat_model>

<verification>
Nach Plan-Abschluss:

```bash
# Backend-Tests
cd /home/spro/development/MentalHealth/backend && uv run pytest tests/test_portability.py -v
cd /home/spro/development/MentalHealth/backend && uv run pytest tests/ -q

# Router registriert?
grep -n "portability" /home/spro/development/MentalHealth/backend/app/main.py

# Kein Registry-Check beim Import?
grep -n "get_module" /home/spro/development/MentalHealth/backend/app/routers/portability.py

# Swagger-Docs zeigen die neuen Endpoints?
# → http://localhost:8000/docs (GET /api/export, POST /api/import sichtbar)
```
</verification>

<success_criteria>
- `uv run pytest tests/test_portability.py -q` → 5 Tests grün
- `uv run pytest tests/ -q` → volle Suite grün
- GET /api/export liefert `{"_version": 1, "_exported": "...", ...modulschlüssel}`
- POST /api/import akzeptiert HTML-v1-JSON inkl. unbekannter Modul-IDs
- `grep "portability" backend/app/main.py` → 2 Treffer
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/03-data-portability/03-01-SUMMARY.md`
</output>
