---
phase: 03-data-portability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api.ts
autonomous: true
requirements:
  - PORT-03

must_haves:
  truths:
    - "serverApi hat eine exportAll()-Methode, die Promise<Record<string, unknown>> zurückgibt"
    - "serverApi hat eine importAll(dump)-Methode, die Promise<void> zurückgibt"
    - "Beide Methoden nutzen den bestehenden request<T>-Helper — kein eigenes fetch"
    - "TypeScript-Compiler akzeptiert den neuen Code ohne Fehler"
    - "localApi und serverApi haben identische Methoden-Signaturen für exportAll/importAll (await-kompatibel)"
  artifacts:
    - path: "frontend/src/api.ts"
      provides: "serverApi um exportAll und importAll erweitert"
      contains: "exportAll"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/api.ts (serverApi.exportAll)"
      via: "await api.exportAll()"
      pattern: "exportAll"
    - from: "frontend/src/api.ts (serverApi.exportAll)"
      to: "GET /api/export"
      via: "request<Record<string, unknown>>(\"/api/export\")"
      pattern: "request.*api/export"
---

<objective>
Erweitere `serverApi` in `frontend/src/api.ts` um `exportAll()` und `importAll()` — die fehlenden Methoden, damit `api.exportAll()` im Server-Modus den Backend-Endpoint statt localStorage aufruft.

Purpose: `App.tsx` kann nach Plan 03 `await api.exportAll()` und `await api.importAll(dump)` aufrufen und erhält automatisch die korrekte Implementierung (server oder lokal).
Output: `frontend/src/api.ts` mit zwei neuen Methoden in `serverApi`. TypeScript-Typecheck grün.
</objective>

<execution_context>
@/home/spro/.claude/get-shit-done/workflows/execute-plan.md
@/home/spro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-data-portability/03-RESEARCH.md
@.planning/phases/03-data-portability/03-PATTERNS.md

<interfaces>
<!-- Aus frontend/src/api.ts — aktuelle Datei (vollständig) -->

```typescript
import { localApi } from "./api.local";
import type { ModuleRecord, ModuleSpecWire } from "./types";

const BASE = (import.meta.env.VITE_API_BASE ?? "http://localhost:8000").replace(/\/$/, "");
const TOKEN = import.meta.env.VITE_KOMPASS_TOKEN ?? "";

const USE_LOCAL =
  import.meta.env.VITE_STORAGE === "local" ||
  (typeof window !== "undefined" && window.location.protocol === "file:");

function headers(extra?: HeadersInit): HeadersInit {
  const h: Record<string, string> = { "Content-Type": "application/json" };
  if (TOKEN) h["Authorization"] = `Bearer ${TOKEN}`;
  return { ...h, ...(extra ?? {}) };
}

async function request<T>(path: string, init?: RequestInit): Promise<T> {
  const res = await fetch(`${BASE}${path}`, { ...init, headers: headers(init?.headers) });
  if (!res.ok) {
    const text = await res.text();
    throw new Error(`${res.status} ${res.statusText}: ${text}`);
  }
  return res.json() as Promise<T>;
}

const serverApi = {
  listModules: () => request<ModuleSpecWire[]>("/api/modules"),
  getModule: <T>(id: string) => request<ModuleRecord<T>>(`/api/modules/${id}`),
  putModule: <T>(id: string, data: T) =>
    request<ModuleRecord<T>>(`/api/modules/${id}`, {
      method: "PUT",
      body: JSON.stringify(data),
    }),
  health: () => request<{ status: string }>("/health"),
  // ← HIER: exportAll und importAll einfügen
};

export const api = USE_LOCAL ? localApi : serverApi;
```

<!-- Aus frontend/src/api.local.ts — Methoden-Signaturen als Typreferenz -->

```typescript
// localApi (synchron, aber als Promise exposed):
exportAll(): Record<string, unknown>  // tatsächlich synchron
importAll(dump: Record<string, unknown>): void  // tatsächlich synchron

// serverApi MUSS async sein (Netzwerk-Call):
exportAll(): Promise<Record<string, unknown>>
importAll(dump: Record<string, unknown>): Promise<void>

// App.tsx ruft beide mit await auf — beide Signaturen sind await-kompatibel
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: serverApi um exportAll und importAll erweitern</name>
  <files>frontend/src/api.ts</files>
  <read_first>
    - frontend/src/api.ts (vollständige aktuelle Datei — Ergänzungspunkt nach health-Methode)
    - frontend/src/api.local.ts (Methoden-Signaturen: exportAll, importAll — Typen übernehmen)
  </read_first>
  <action>
    In `frontend/src/api.ts` das `serverApi`-Objekt um zwei Methoden erweitern. Die Methoden kommen nach `health:`, vor der schließenden `}` des Objekts.

    Exakte Ergänzung:
    ```typescript
    const serverApi = {
      listModules: () => request<ModuleSpecWire[]>("/api/modules"),
      getModule: <T>(id: string) => request<ModuleRecord<T>>(`/api/modules/${id}`),
      putModule: <T>(id: string, data: T) =>
        request<ModuleRecord<T>>(`/api/modules/${id}`, {
          method: "PUT",
          body: JSON.stringify(data),
        }),
      health: () => request<{ status: string }>("/health"),
      exportAll: (): Promise<Record<string, unknown>> =>
        request<Record<string, unknown>>("/api/export"),
      importAll: (dump: Record<string, unknown>): Promise<void> =>
        request<void>("/api/import", {
          method: "POST",
          body: JSON.stringify(dump),
        }),
    };
    ```

    Kein neues fetch bauen — ausschließlich über `request<T>()` arbeiten.
    Keine anderen Zeilen in der Datei ändern.
    `localApi`-Import bleibt (wird für `api = USE_LOCAL ? localApi : serverApi` gebraucht).
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/frontend && npm run type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "exportAll" frontend/src/api.ts` liefert mindestens 1 Treffer
    - `grep -n "importAll" frontend/src/api.ts` liefert mindestens 1 Treffer
    - `grep -n "request.*api/export" frontend/src/api.ts` liefert 1 Treffer
    - `grep -n "request.*api/import" frontend/src/api.ts` liefert 1 Treffer
    - `cd frontend && npm run type-check` exitiert mit Code 0 (kein TypeScript-Fehler)
  </acceptance_criteria>
  <done>
    `serverApi` enthält `exportAll` und `importAll`. TypeScript-Compiler akzeptiert die Datei fehlerfrei. `api.exportAll()` gibt `Promise<Record<string, unknown>>` zurück — await-kompatibel mit localApi-Signaturen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Browser → /api/export | Bearer-Token in Header; ohne Token → 401 (Backend-seitig abgesichert) |
| Browser → /api/import | importAll sendet beliebiges JSON; Validierung liegt im Backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | serverApi.exportAll | accept | Frontend leitet nur weiter; Datenschutz liegt in Backend-Auth (T-03-02 in Plan 01) |
| T-03-06 | Tampering | serverApi.importAll | accept | Frontend sendet rohen JSON-Blob; Backend entscheidet über Akzeptanz (T-03-04 in Plan 01) |
</threat_model>

<verification>
```bash
# TypeScript sauber?
cd /home/spro/development/MentalHealth/frontend && npm run type-check

# Methoden vorhanden?
grep -n "exportAll\|importAll" /home/spro/development/MentalHealth/frontend/src/api.ts

# Nutzen sie request<T>?
grep -n "request.*api/export\|request.*api/import" /home/spro/development/MentalHealth/frontend/src/api.ts
```
</verification>

<success_criteria>
- `npm run type-check` → 0 Fehler
- `grep "exportAll" frontend/src/api.ts` → 1 Treffer in serverApi
- `grep "importAll" frontend/src/api.ts` → 1 Treffer in serverApi
- Beide Methoden nutzen ausschließlich `request<T>()` (kein eigenes fetch)
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/03-data-portability/03-02-SUMMARY.md`
</output>
