---
phase: 02-content-gaps
plan: 08
type: execute
wave: 5
depends_on: [02-07]
files_modified:
  - frontend/src/modules/ysq/constants.ts
autonomous: false
requirements: [CONT-02, CONT-03]

must_haves:
  truths:
    - "Each of the 18 schemas in YSQ_SCHEMAS has exactly 5 non-empty item strings"
    - "YSQ_MAX_ITEM_SCORE reflects the actual scale endpoint from reference/kompass.html"
    - "Schema labels and item texts are in German and match reference/kompass.html exactly"
    - "After update, all 90 items have content — no empty string remains in any items array"
  artifacts:
    - path: "frontend/src/modules/ysq/constants.ts"
      provides: "YSQ_SCHEMAS with real item texts from reference/kompass.html"
      contains: "YSQ_MAX_ITEM_SCORE"
  key_links:
    - from: "frontend/src/modules/ysq/constants.ts"
      to: "reference/kompass.html"
      via: "Item texts copied verbatim from the HTML reference"
      pattern: "items: \\[\"[^\"]+\""
---

<objective>
Populate `ysq/constants.ts` with the real YSQ-S3 item texts from `reference/kompass.html` and confirm the Likert scale endpoint (CONT-02, CONT-03 content completion).

Purpose: Plans 05-07 created the YSQ module with 18 schema entries that have empty `items: ["","","","",""]` arrays. This plan fills those placeholder strings with the actual German item texts from the reference HTML file. It also confirms whether the scale is 1–4 or 1–6 and updates `YSQ_MAX_ITEM_SCORE` accordingly.

CRITICAL BLOCKER: This plan requires `reference/kompass.html` to be present on disk. Run `ls reference/kompass.html` before starting. If the file is absent, STOP and notify the user — do not create placeholder text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-content-gaps/02-CONTEXT.md

<interfaces>
<!-- Current constants.ts structure — executor must read the actual file for full content. -->

From frontend/src/modules/ysq/constants.ts (current skeleton — will be replaced with real content):
```typescript
export const YSQ_SCHEMAS: YsqSchema[] = [
  { id: "abandonment", label: "Verlassenheit / Instabilität", items: ["", "", "", "", ""] },
  // ... 17 more with empty items
];
export const YSQ_MAX_ITEM_SCORE = 4; // ASSUMED — verify from reference/kompass.html
export const YSQ_MAX_SCHEMA_SCORE = 5 * YSQ_MAX_ITEM_SCORE;
```

Schema ID → German label mapping (IDs must stay unchanged from plan-05):
```
abandonment → Verlassenheit / Instabilität
mistrust → Misstrauen / Missbrauch
emotional_deprivation → Emotionale Entbehrung
defectiveness → Unzulänglichkeit / Scham
social_isolation → Soziale Isolation / Entfremdung
dependence → Abhängigkeit / Inkompetenz
vulnerability → Anfälligkeit für Schaden oder Krankheit
enmeshment → Verstrickung / Unterentwickeltes Selbst
failure → Versagen
entitlement → Anspruchlichkeit / Grandiosität
insufficient_self_control → Unzureichende Selbstkontrolle
subjugation → Unterwerfung
self_sacrifice → Selbstaufopferung
approval_seeking → Streben nach Zustimmung
negativity → Negativität / Pessimismus
emotional_inhibition → Emotionale Gehemmtheit
unrelenting_standards → Hohe Standards / Überkritik
punitiveness → Bestrafen
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Nothing yet — this checkpoint verifies the prerequisite file exists before any code changes are made.</what-built>
  <how-to-verify>
    Run:
    ```bash
    ls -la /home/spro/development/MentalHealth/reference/kompass.html
    ```
    Expected: The file exists and is non-empty (size > 0 bytes).

    If the file does NOT exist:
    - Stop this plan
    - Place the file at `reference/kompass.html`
    - Then resume by re-running plan-08

    If the file exists: proceed to the next task.
  </how-to-verify>
  <resume-signal>Type "file present" to proceed, or "file missing" to stop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extract YSQ items from reference/kompass.html and populate constants.ts</name>
  <read_first>
    - reference/kompass.html (read the YSQ section — find the 90 item texts in order; they are likely in a JS data structure or HTML form. Scan for the questionnaire items, schema names, and Likert scale labels)
    - frontend/src/modules/ysq/constants.ts (full file — read current content before rewriting)
  </read_first>
  <files>frontend/src/modules/ysq/constants.ts</files>
  <action>
    1. Read `reference/kompass.html` and locate:
       - The 90 YSQ-S3 item texts in German (5 per schema, 18 schemas)
       - The Likert scale options and their numeric values (confirm whether scale is 1–4, 1–5, or 1–6)
       - Confirm the schema order matches the 18 schema IDs established in plan-05 (abandonment first, punitiveness last)

    2. Update `frontend/src/modules/ysq/constants.ts`:
       - Replace each `items: ["", "", "", "", ""]` with the 5 actual item texts for that schema
       - Item texts must be copied verbatim — do not rephrase, translate, or shorten
       - Update `YSQ_MAX_ITEM_SCORE` to the actual scale maximum (e.g., 4 if scale is 1–4, 6 if scale is 1–6)
       - If schema labels in reference/kompass.html differ from the labels in constants.ts, update the `label` strings to match the reference exactly
       - Do NOT change schema IDs (`id` field) — they are stable identifiers used in data storage
       - Do NOT change the array length (must remain 18 entries)
       - Do NOT change the export names or structure

    3. Verify the update:
       - Every `items` array has exactly 5 non-empty strings
       - `YSQ_MAX_ITEM_SCORE` matches the scale endpoint from the reference
       - `YSQ_MAX_SCHEMA_SCORE = 5 * YSQ_MAX_ITEM_SCORE` is consistent

    If the schema order in reference/kompass.html differs from the order in plan-05's constants.ts:
    - Reorder the entries to match reference/kompass.html
    - NOTE: Reordering changes which answer array index maps to which schema. This only affects display (all existing data uses the committed `answers` array indexed by position). For a fresh install with no existing data, reordering is safe.
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit 2>&1 && node -e "const s = require('./src/modules/ysq/constants.ts'); console.log('schemas:', s.YSQ_SCHEMAS?.length ?? 'error')" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '""' frontend/src/modules/ysq/constants.ts` returns 0 (no empty strings remain in items arrays)
    - `grep -c "items: \[" frontend/src/modules/ysq/constants.ts` returns 18 (one items array per schema)
    - `grep "YSQ_MAX_ITEM_SCORE" frontend/src/modules/ysq/constants.ts` returns a line with a numeric value (not just 4 if the actual scale is different)
    - `cd frontend && npx tsc --noEmit` exits 0
    - Running the frontend development server and navigating to the YSQ module shows German item texts on schema page 1 (manual verification in the checkpoint below)
  </acceptance_criteria>
  <done>All 90 YSQ items populated from reference/kompass.html; YSQ_MAX_ITEM_SCORE correct; TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    constants.ts updated with all 90 real YSQ item texts from reference/kompass.html.
    YSQ_MAX_ITEM_SCORE set to the confirmed scale endpoint.
  </what-built>
  <how-to-verify>
    1. Start the frontend dev server:
       ```bash
       cd /home/spro/development/MentalHealth/frontend && npm run dev
       ```
    2. Open the app in a browser and navigate to the YSQ module.
    3. Verify:
       - Schema page 1 (Verlassenheit / Instabilität or the first schema in the reference) shows 5 real German item texts
       - The answer buttons show the correct Likert scale labels from the reference
       - The progress bar shows "Schema 1 von 18" in the header
    4. Compare 3-5 item texts against `reference/kompass.html` to confirm they match verbatim.
  </how-to-verify>
  <resume-signal>Type "items verified" if the item texts match the reference, or describe any discrepancies.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference/kompass.html → constants.ts | Trusted source file on developer's local disk; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | YSQ item text accuracy | accept | Item texts come from a trusted local reference file. The only risk is a copy error during extraction. Mitigated by the human verification checkpoint at the end of this plan. |
</threat_model>

<verification>
```bash
grep -c '""' /home/spro/development/MentalHealth/frontend/src/modules/ysq/constants.ts
```
Expect: 0 (no empty strings remaining).

```bash
cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit
```
Expect: exit code 0.
</verification>

<success_criteria>
- `reference/kompass.html` verified present on disk before any changes
- All 90 YSQ item texts populated from reference/kompass.html (no empty strings)
- `YSQ_MAX_ITEM_SCORE` updated to the confirmed scale value from reference
- `cd frontend && npx tsc --noEmit` exits 0
- Human verification confirms item texts match reference in at least one schema page
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-gaps/02-08-SUMMARY.md`
</output>
