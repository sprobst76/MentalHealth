---
phase: 02-content-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/modules/goals/constants.ts
  - frontend/src/modules/goals/GoalsModule.tsx
  - frontend/src/modules/beliefs_act/constants.ts
  - frontend/src/modules/beliefs_act/BeliefsActModule.tsx
  - frontend/src/modules/obstacles/constants.ts
  - frontend/src/modules/obstacles/ObstaclesModule.tsx
autonomous: true
requirements: [CONT-06]

must_haves:
  truths:
    - "GOAL_PROMPTS, DEFUSION_EXAMPLES, and EXPLORATION_PROMPTS are exported from constants.ts files, not declared inside component function bodies"
    - "The three component files import the constants and compile without TypeScript errors"
    - "No runtime behavior changes — prompt text is identical before and after the move"
  artifacts:
    - path: "frontend/src/modules/beliefs_act/constants.ts"
      provides: "DEFUSION_EXAMPLES string array (5 items)"
      exports: ["DEFUSION_EXAMPLES"]
    - path: "frontend/src/modules/obstacles/constants.ts"
      provides: "EXPLORATION_PROMPTS string array (5 items)"
      exports: ["EXPLORATION_PROMPTS"]
    - path: "frontend/src/modules/goals/constants.ts"
      provides: "GOAL_PROMPTS appended to existing HORIZON_LABEL and STATUS_LABEL exports"
      exports: ["GOAL_PROMPTS", "HORIZON_LABEL", "STATUS_LABEL"]
  key_links:
    - from: "frontend/src/modules/beliefs_act/BeliefsActModule.tsx"
      to: "frontend/src/modules/beliefs_act/constants.ts"
      via: "import { DEFUSION_EXAMPLES } from './constants'"
      pattern: "import.*DEFUSION_EXAMPLES.*from.*constants"
    - from: "frontend/src/modules/obstacles/ObstaclesModule.tsx"
      to: "frontend/src/modules/obstacles/constants.ts"
      via: "import { EXPLORATION_PROMPTS } from './constants'"
      pattern: "import.*EXPLORATION_PROMPTS.*from.*constants"
    - from: "frontend/src/modules/goals/GoalsModule.tsx"
      to: "frontend/src/modules/goals/constants.ts"
      via: "import { HORIZON_LABEL, STATUS_LABEL, GOAL_PROMPTS } from './constants'"
      pattern: "import.*GOAL_PROMPTS.*from.*constants"
---

<objective>
Extract three hardcoded prompt arrays from component bodies into proper `constants.ts` files (CONT-06).

Purpose: Inline constant arrays in component bodies cannot be tree-shaken, tested independently, or reused by other modules. Moving them to `constants.ts` follows the established project convention (see `goals/constants.ts` which already holds `HORIZON_LABEL` and `STATUS_LABEL`).

Output: Three constants files with the arrays, three component files updated to import from them. Zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-content-gaps/02-CONTEXT.md

<interfaces>
<!-- Key patterns the executor needs. Do not re-read these files beyond this block. -->

From frontend/src/modules/goals/constants.ts (current state — extend this file):
```typescript
import type { Horizon, GoalStatus } from "./types";

export const HORIZON_LABEL: Record<Horizon, string> = {
  "30days": "30 Tage",
  quarter: "3 Monate",
  year: "1 Jahr",
  longer: "länger",
};

export const STATUS_LABEL: Record<GoalStatus, string> = {
  active: "Aktiv",
  achieved: "Erreicht",
  paused: "Pausiert",
};
```

From frontend/src/modules/goals/GoalsModule.tsx lines 1-20 (current import + inline array):
```typescript
import { HORIZON_LABEL, STATUS_LABEL } from "./constants";
// ...
const GOAL_PROMPTS = [
  "Was genau willst du erreichen — so konkret, dass du es beschreiben könntest?",
  "Woran wirst du erkennen, dass du es erreicht hast?",
  "Warum ist dieses Ziel wichtig für dich — welchen Wert lebt es?",
  "Was könnte dich aufhalten? (Das ist Vorbereitung, keine Niederlage.)",
  "Was wäre dein allererster Schritt — heute oder diese Woche?",
];
```

From frontend/src/modules/beliefs_act/BeliefsActModule.tsx lines 10-16 (inline array to move):
```typescript
const DEFUSION_EXAMPLES = [
  "Ich bemerke, dass ich den Gedanken habe, dass …",
  "Mein Kopf erzählt mir gerade die Geschichte von …",
  "Danke, Kopf — ich kenne diesen Gedanken. Ich muss ihm aber nicht folgen.",
  "Ich beobachte, wie dieser Gedanke kommt und geht — wie eine Wolke.",
  "Das ist nur ein Gedanke, keine Tatsache.",
];
```

From frontend/src/modules/obstacles/ObstaclesModule.tsx lines 12-18 (inline array to move):
```typescript
const EXPLORATION_PROMPTS = [
  "Wie alt fühlst du dich in diesem Moment, wenn das Hindernis auftaucht?",
  "Gibt es eine innere Stimme — klingt sie vertraut? Wessen Stimme könnte das sein?",
  "Was hat der Teil von dir, der dieses Muster entwickelt hat, damals gebraucht?",
  "Was wäre ein kleiner Schritt, der dir zeigt, dass du diesem Muster nicht folgen musst?",
  "Wie kannst du dir selbst in diesem Moment Mitgefühl entgegenbringen?",
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract GOAL_PROMPTS to goals/constants.ts</name>
  <read_first>
    - frontend/src/modules/goals/constants.ts (full file — see current exports, append after them)
    - frontend/src/modules/goals/GoalsModule.tsx (find inline GOAL_PROMPTS declaration and the import line for constants)
  </read_first>
  <files>frontend/src/modules/goals/constants.ts, frontend/src/modules/goals/GoalsModule.tsx</files>
  <action>
    1. Append to `frontend/src/modules/goals/constants.ts` (after the STATUS_LABEL export):
       ```typescript
       export const GOAL_PROMPTS: string[] = [
         "Was genau willst du erreichen — so konkret, dass du es beschreiben könntest?",
         "Woran wirst du erkennen, dass du es erreicht hast?",
         "Warum ist dieses Ziel wichtig für dich — welchen Wert lebt es?",
         "Was könnte dich aufhalten? (Das ist Vorbereitung, keine Niederlage.)",
         "Was wäre dein allererster Schritt — heute oder diese Woche?",
       ];
       ```
    2. In `frontend/src/modules/goals/GoalsModule.tsx`:
       - Remove the inline `const GOAL_PROMPTS = [...]` block (lines 14-20)
       - Update the import from `"./constants"` to add `GOAL_PROMPTS`: `import { HORIZON_LABEL, STATUS_LABEL, GOAL_PROMPTS } from "./constants";`
       - No other changes to GoalsModule.tsx
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GOAL_PROMPTS" frontend/src/modules/goals/constants.ts` returns a line with `export const GOAL_PROMPTS: string[]`
    - `grep -n "GOAL_PROMPTS" frontend/src/modules/goals/GoalsModule.tsx` shows only the import line, NOT a `const GOAL_PROMPTS = [` declaration
    - `grep -n "GOAL_PROMPTS" frontend/src/modules/goals/GoalsModule.tsx` returns a line matching `import.*GOAL_PROMPTS.*from.*constants`
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GOAL_PROMPTS is exported from goals/constants.ts; GoalsModule.tsx imports it; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract DEFUSION_EXAMPLES and EXPLORATION_PROMPTS to new constants files</name>
  <read_first>
    - frontend/src/modules/beliefs_act/BeliefsActModule.tsx (find inline DEFUSION_EXAMPLES declaration and full import block at top)
    - frontend/src/modules/obstacles/ObstaclesModule.tsx (find inline EXPLORATION_PROMPTS declaration and full import block at top)
  </read_first>
  <files>
    frontend/src/modules/beliefs_act/constants.ts,
    frontend/src/modules/beliefs_act/BeliefsActModule.tsx,
    frontend/src/modules/obstacles/constants.ts,
    frontend/src/modules/obstacles/ObstaclesModule.tsx
  </files>
  <action>
    1. Create `frontend/src/modules/beliefs_act/constants.ts` (new file):
       ```typescript
       export const DEFUSION_EXAMPLES: string[] = [
         "Ich bemerke, dass ich den Gedanken habe, dass …",
         "Mein Kopf erzählt mir gerade die Geschichte von …",
         "Danke, Kopf — ich kenne diesen Gedanken. Ich muss ihm aber nicht folgen.",
         "Ich beobachte, wie dieser Gedanke kommt und geht — wie eine Wolke.",
         "Das ist nur ein Gedanke, keine Tatsache.",
       ];
       ```
       Note: Use the exact German string values from the inline array (copy them verbatim — do not paraphrase).

    2. In `frontend/src/modules/beliefs_act/BeliefsActModule.tsx`:
       - Remove the inline `const DEFUSION_EXAMPLES = [...]` block (lines 10-16)
       - Add `import { DEFUSION_EXAMPLES } from "./constants";` to the import section (after other local imports)

    3. Create `frontend/src/modules/obstacles/constants.ts` (new file):
       ```typescript
       export const EXPLORATION_PROMPTS: string[] = [
         "Wie alt fühlst du dich in diesem Moment, wenn das Hindernis auftaucht?",
         "Gibt es eine innere Stimme — klingt sie vertraut? Wessen Stimme könnte das sein?",
         "Was hat der Teil von dir, der dieses Muster entwickelt hat, damals gebraucht?",
         "Was wäre ein kleiner Schritt, der dir zeigt, dass du diesem Muster nicht folgen musst?",
         "Wie kannst du dir selbst in diesem Moment Mitgefühl entgegenbringen?",
       ];
       ```
       Note: Copy the exact German string values verbatim from the inline array.

    4. In `frontend/src/modules/obstacles/ObstaclesModule.tsx`:
       - Remove the inline `const EXPLORATION_PROMPTS = [...]` block (lines 12-18)
       - Add `import { EXPLORATION_PROMPTS } from "./constants";` to the import section

    Convention: All string values must be byte-for-byte identical to the originals (German punctuation, em-dashes, ellipses).
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/modules/beliefs_act/constants.ts` — file exists
    - `ls frontend/src/modules/obstacles/constants.ts` — file exists
    - `grep "export const DEFUSION_EXAMPLES" frontend/src/modules/beliefs_act/constants.ts` returns a match
    - `grep "export const EXPLORATION_PROMPTS" frontend/src/modules/obstacles/constants.ts` returns a match
    - `grep "const DEFUSION_EXAMPLES" frontend/src/modules/beliefs_act/BeliefsActModule.tsx` returns NO match (inline declaration gone)
    - `grep "const EXPLORATION_PROMPTS" frontend/src/modules/obstacles/ObstaclesModule.tsx` returns NO match (inline declaration gone)
    - `grep "import.*DEFUSION_EXAMPLES.*from.*constants" frontend/src/modules/beliefs_act/BeliefsActModule.tsx` returns a match
    - `grep "import.*EXPLORATION_PROMPTS.*from.*constants" frontend/src/modules/obstacles/ObstaclesModule.tsx` returns a match
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both new constants files created with correct exports; component files import from them; TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan is a pure frontend refactor — no data flows change, no new API calls, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | constants.ts string values | accept | Strings are static compile-time values; no runtime input; risk = typo during extraction. Mitigated by grep acceptance criteria verifying the import/export wiring and tsc confirming no compile error. |
</threat_model>

<verification>
Run after both tasks:
```bash
cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit
```
Expect: exit code 0, no output.
</verification>

<success_criteria>
- `frontend/src/modules/beliefs_act/constants.ts` exists with `export const DEFUSION_EXAMPLES: string[]`
- `frontend/src/modules/obstacles/constants.ts` exists with `export const EXPLORATION_PROMPTS: string[]`
- `frontend/src/modules/goals/constants.ts` contains `export const GOAL_PROMPTS: string[]`
- No inline `const GOAL_PROMPTS`, `const DEFUSION_EXAMPLES`, or `const EXPLORATION_PROMPTS` remain in component files
- All three component files import their respective constant from `"./constants"`
- `cd frontend && npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-gaps/02-01-SUMMARY.md`
</output>
