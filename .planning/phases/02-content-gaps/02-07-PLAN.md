---
phase: 02-content-gaps
plan: 07
type: execute
wave: 4
depends_on: [02-05]
files_modified:
  - frontend/src/modules/ysq/YsqSummary.tsx
  - frontend/src/modules/registry.ts
autonomous: true
requirements: [CONT-05]

must_haves:
  truths:
    - "YsqSummary renders a top-3 schema list when data.answers is non-null"
    - "YsqSummary renders an empty-state italic paragraph when data.answers is null"
    - "ysqModule appears in the frontend registry modules array between checkinModule and orientationModule"
    - "TypeScript compiles without errors after both files are updated"
  artifacts:
    - path: "frontend/src/modules/ysq/YsqSummary.tsx"
      provides: "YsqSummary component for Synthese page"
      exports: ["YsqSummary"]
    - path: "frontend/src/modules/registry.ts"
      provides: "ysqModule registered in modules array"
      contains: "import { ysqModule } from \"./ysq\""
  key_links:
    - from: "frontend/src/modules/ysq/YsqSummary.tsx"
      to: "frontend/src/modules/ysq/constants.ts"
      via: "import { YSQ_SCHEMAS } from './constants'"
      pattern: "import.*YSQ_SCHEMAS.*from.*constants"
    - from: "frontend/src/modules/registry.ts"
      to: "frontend/src/modules/ysq/index.ts"
      via: "import { ysqModule } from './ysq'"
      pattern: "import.*ysqModule.*from.*ysq"
---

<objective>
Create `YsqSummary.tsx` (the Synthese-page summary block for YSQ, CONT-05) and register `ysqModule` in the frontend registry (completing CONT-03 wiring).

Purpose: After plan-06 creates `YsqModule`, the `index.ts` created in plan-05 can now be safely imported by the registry. This plan completes the YSQ module installation so it is navigable from the sidebar.

Output: `YsqSummary.tsx` showing top-3 schemas by score, and `registry.ts` with `ysqModule` inserted after `checkinModule`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-gaps/02-UI-SPEC.md
@.planning/phases/02-content-gaps/02-CONTEXT.md

<interfaces>
<!-- Exact contracts the executor needs. Read the files in read_first; use patterns below. -->

From frontend/src/modules/checkin/CheckinSummary.tsx (empty state + list pattern to mirror):
```typescript
// Minimal props interface pattern:
interface Props {
  data: CheckinData;
}
export function CheckinSummary({ data }: Props) {
  if (data.entries.length === 0) {
    return (
      <p className="text-ink-faint italic">
        Noch kein Check-in erfasst. Starte im Modul{' '}
        {'„Wochen-Check-in"'}.
      </p>
    );
  }
  // ...list rendering
}
```

From frontend/src/modules/ysq/types.ts (already created in plan-05):
```typescript
export type YsqAnswer = number | null;
export interface YsqData {
  answers: YsqAnswer[] | null;
  draft: YsqAnswer[] | null;
  notes: Record<string, string>;
}
```

From frontend/src/modules/ysq/constants.ts (already created in plan-05):
```typescript
export const YSQ_SCHEMAS: YsqSchema[];  // 18 entries
```

From frontend/src/modules/registry.ts (current modules array):
```typescript
export const modules: ModuleDef[] = [
  checkinModule,
  orientationModule,
  valuesModule,
  beliefsSchemaModule,
  beliefsActModule,
  goalsModule,
  obstaclesModule,
  syntheseModule,
];
```

UI-SPEC summary block contract (Section 6):
- Empty state: `<p className="text-sm text-ink-faint italic">Noch kein YSQ ausgefüllt.</p>`
- Results heading: `<h3 className="display text-xl mb-3">Auffälligste Schemata</h3>`
- Each of top-3 rows:
  ```tsx
  <div className="flex items-center justify-between py-2 border-b border-line-soft last:border-b-0">
    <span className="text-sm text-ink">{schemaName}</span>
    <span className="text-xs font-mono text-ink-faint">{score}</span>
  </div>
  ```
- No bar chart in summary — text list only (consistent with ValuesSummary compact style)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YsqSummary.tsx</name>
  <read_first>
    - frontend/src/modules/checkin/CheckinSummary.tsx (full file — mirror structure for empty state and list rendering)
    - frontend/src/modules/ysq/types.ts (confirm YsqData interface)
    - frontend/src/modules/ysq/constants.ts (confirm YSQ_SCHEMAS export)
  </read_first>
  <files>frontend/src/modules/ysq/YsqSummary.tsx</files>
  <action>
    Create `frontend/src/modules/ysq/YsqSummary.tsx`:

    ```typescript
    import { YSQ_SCHEMAS } from "./constants";
    import type { YsqData } from "./types";

    interface Props {
      data: YsqData;
    }

    export function YsqSummary({ data }: Props) {
      if (data.answers == null) {
        return (
          <p className="text-sm text-ink-faint italic">Noch kein YSQ ausgefüllt.</p>
        );
      }

      // Compute per-schema scores; null = all 5 items were null (skipped)
      const scored = YSQ_SCHEMAS.map((schema, i) => {
        const items = data.answers!.slice(i * 5, i * 5 + 5);
        const allNull = items.every((v) => v === null);
        const score = allNull ? null : items.reduce((sum, v) => sum + (v ?? 0), 0);
        return { schema, score };
      });

      // Sort descending, excluding skipped schemas, take top 3
      const top3 = scored
        .filter((s) => s.score !== null)
        .sort((a, b) => (b.score ?? 0) - (a.score ?? 0))
        .slice(0, 3);

      if (top3.length === 0) {
        return (
          <p className="text-sm text-ink-faint italic">Noch kein YSQ ausgefüllt.</p>
        );
      }

      return (
        <div>
          <h3 className="display text-xl mb-3">Auffälligste Schemata</h3>
          <div>
            {top3.map(({ schema, score }) => (
              <div
                key={schema.id}
                className="flex items-center justify-between py-2 border-b border-line-soft last:border-b-0"
              >
                <span className="text-sm text-ink">{schema.label}</span>
                <span className="text-xs font-mono text-ink-faint">{score}</span>
              </div>
            ))}
          </div>
        </div>
      );
    }
    ```

    Notes:
    - `data.answers!` is safe after the `data.answers == null` guard above
    - Partial item completion (some items null within a schema): sum non-null items, treat schema as scored (not skipped) — this matches the design decision in RESEARCH.md assumption A1
    - No emojis, no `rounded-lg`
    - German quotation marks not needed in this component (no `„"` constructs used)
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit 2>&1 | grep "YsqSummary" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/modules/ysq/YsqSummary.tsx` — file exists
    - `grep "export function YsqSummary" frontend/src/modules/ysq/YsqSummary.tsx` returns a match
    - `grep "data.answers == null" frontend/src/modules/ysq/YsqSummary.tsx` returns a match (empty state guard)
    - `grep "Auffälligste Schemata" frontend/src/modules/ysq/YsqSummary.tsx` returns a match
    - `grep "Noch kein YSQ ausgefüllt" frontend/src/modules/ysq/YsqSummary.tsx` returns a match (empty state text)
    - `grep "\.slice(0, 3)" frontend/src/modules/ysq/YsqSummary.tsx` returns a match (top-3 selection)
  </acceptance_criteria>
  <done>YsqSummary.tsx created; renders empty state when answers is null; renders top-3 sorted schemas as a text list when answers exist.</done>
</task>

<task type="auto">
  <name>Task 2: Register ysqModule in frontend registry and verify TypeScript</name>
  <read_first>
    - frontend/src/modules/registry.ts (full file — read current modules array before modifying)
    - frontend/src/modules/ysq/index.ts (confirm ysqModule export exists from plan-05)
  </read_first>
  <files>frontend/src/modules/registry.ts</files>
  <action>
    Modify `frontend/src/modules/registry.ts`:

    1. Add the ysqModule import with the other module imports (alphabetical order is not required; add after the checkin import):
       ```typescript
       import { ysqModule } from "./ysq";
       ```

    2. Insert `ysqModule` in the `modules` array after `checkinModule`:
       ```typescript
       export const modules: ModuleDef[] = [
         checkinModule,
         ysqModule,           // CONT-03: insert here — both are structured questionnaires
         orientationModule,
         valuesModule,
         beliefsSchemaModule,
         beliefsActModule,
         goalsModule,
         obstaclesModule,
         syntheseModule,
       ];
       ```

    No other changes to registry.ts.
  </action>
  <verify>
    <automated>cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { ysqModule } from \"./ysq\"" frontend/src/modules/registry.ts` returns a match
    - `grep "ysqModule," frontend/src/modules/registry.ts` returns a match
    - The ysqModule entry in the array comes after checkinModule and before orientationModule (verify position with `grep -n "checkinModule\|ysqModule\|orientationModule" frontend/src/modules/registry.ts`)
    - `cd frontend && npx tsc --noEmit` exits 0 with no output
  </acceptance_criteria>
  <done>ysqModule registered in frontend registry; TypeScript compiles cleanly across all ysq module files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YsqSummary reads data.answers from allData | Read-only display of existing data — no new input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Information Disclosure | YsqSummary on Synthese page | accept | Shows top-3 schema names + scores to the same user who entered the data. Single-user tool — no cross-user disclosure risk. |
</threat_model>

<verification>
```bash
cd /home/spro/development/MentalHealth/frontend && npx tsc --noEmit 2>&1
```
Expect: exit code 0, no output. This is the first full clean compile that includes all ysq files.
</verification>

<success_criteria>
- `frontend/src/modules/ysq/YsqSummary.tsx` renders top-3 schemas in a text list, empty state when no answers
- `frontend/src/modules/registry.ts` imports and includes `ysqModule` after `checkinModule`
- `cd frontend && npx tsc --noEmit` exits 0 (all ysq types resolve, no compile errors)
- YSQ module is now navigable from the sidebar (registered in frontend registry)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-gaps/02-07-SUMMARY.md`
</output>
