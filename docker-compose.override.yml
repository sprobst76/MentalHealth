services:
  backend:
    ports:
      - "18000:8000"
    environment:
      CORS_ORIGINS: http://localhost:15173

  frontend:
    ports:
      - "15173:5173"
    environment:
      VITE_API_BASE: http://localhost:18000
